## 🚀 Desafio 02 - UniÚnica

Neste desafio, utilizei uma lista de feriados como dados mocados, em vez de consumir uma API externa.

Para executar o projeto no terminal do VS Code, é necessário ter o Node.js instalado.
Após inserir o código, basta rodar o comando:

```bash
node index.js
```

Para validar os cenários de feriados, é possível alterar a data do computador e executar o aplicativo normalmente.
